# FRAMPP Redis 配置（由 init.ps1 生成）
bind 127.0.0.1
port 6379
protected-mode yes
requirepass {{REDIS_PASSWORD}}
appendonly yes
appendfilename "appendonly.aof"
dir {{DATA_DIR}}
logfile {{LOG_FILE}}
daemonize no
